import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  textPlace: {
    fontWeight: 'bold',
    fontSize: 16,
  },
  containerComponent: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingTop: 10,
  },
});

export default styles;
