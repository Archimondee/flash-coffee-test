import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  textPlace: {
    fontWeight: 'bold',
    fontSize: 14,
  },
  containerComponent: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingTop: 5,
  },
});

export default styles;
