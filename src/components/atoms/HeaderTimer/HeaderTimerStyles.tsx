import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingVertical: 60,
  },
  textDate: {
    fontSize: 20,
    paddingTop: 10,
    paddingBottom: 20,
  },
  textTimer: {
    fontSize: 60,
  },
});

export default styles;
