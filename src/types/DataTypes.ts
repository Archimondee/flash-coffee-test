export interface Schedule {
  day: string;
  date: string;
}

export interface UpcomingScheduleType {
  day: string;
  date: string;
  month: string;
  year: string;
  place: string;
  isToday: boolean;
}
